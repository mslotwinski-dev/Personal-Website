export enum Degree {
    BACHELOR = 'Bachelor',
    MASTER = 'Master',
    DOCTOR = 'Doctor'
}

export enum LangType {
    FRONTEND = 'Frontend',
    BACKEND = 'Backend',
    DATABASE = 'Database'
}

export enum ProjectType {
    QUIZZES = 'Quizzes',
    PERSONAL = 'Personal',
    MINOR = 'Minor'
}
