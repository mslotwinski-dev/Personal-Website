export enum Degree {
  BACHELOR = 'Bachelor',
  MASTER = 'Master',
  DOCTOR = 'Doctor',
}

export enum LangType {
  FRONTEND = 'Frontend',
  BACKEND = 'Backend',
  DATABASE = 'Database',
}

export enum ProjectType {
  NONPROFIT = 'Non Profit',
  QUIZZES = 'Quizzes',
  PERSONAL = 'Personal',
  MINOR = 'Minor',
}
